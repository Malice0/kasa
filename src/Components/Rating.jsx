import pinkstart from '../assets/pinkStart.svg'
import greystart from '../assets/greyStart.svg'

const Rating = ({ rating }) => {
    //Construction d'un Array de 5 éléments qu'on rempli avec les étoiles
    const rate = []

    // rate = 
}

export default Rating
